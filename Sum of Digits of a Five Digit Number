#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
   
    int n,calc;
    int sum;
    scanf("%d", &n);
    for(int i=1;i<=5;i++)
    {
     calc=n%10;
    sum+=calc;
     n=n/10;
}
printf("%d",sum);
return 0;
    
}
